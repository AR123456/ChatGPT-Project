import { useState } from "react";

const Modal = () => {
  const [error, setError] = useState(null);
  return (
    <div className="modal">
      <div>modal</div>
    </div>
  );
};

export default Modal;
